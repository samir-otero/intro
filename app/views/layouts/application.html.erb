<!DOCTYPE html>
<html>
  <head>
    <title>Rick and Morty Database</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <!-- Navigation Menu -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to "Rick and Morty DB", root_path, class: "navbar-brand" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Characters", characters_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Locations", locations_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Episodes", episodes_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "About", about_path, class: "nav-link" %>
            </li>
          </ul>

          <!-- Search Form -->
          <div class="d-flex">
            <%= form_with url: characters_path, method: :get, local: true, class: "d-flex" do |f| %>
              <%= f.text_field :search, placeholder: "Search characters...", value: params[:search], class: "form-control me-2" %>
              <%= f.submit "Search", class: "btn btn-outline-light" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="container mt-4">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light text-center py-3 mt-5">
      <div class="container">
        <p>&copy; 2025 Rick and Morty Database - Data from <a href="https://rickandmortyapi.com/" target="_blank" class="text-light">Rick and Morty API</a></p>
      </div>
    </footer>
  </body>
</html>
